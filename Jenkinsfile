void setBuildStatus(String context, String message, String state) {
}

pipeline {
    agent none
    stages {

        stage('Build2') {
            steps {
               echo "hello world"
            }    
        }
    }
}
